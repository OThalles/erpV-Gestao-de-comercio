<div class="top-buttons">
    <h2 class="name-page" style="color:#6c757d">{{$name}}</h2>
    @if($button)
        <a href="{{route($routes)}}"><div class="default-button-2">{{$namebuttons}}</div></a>
    @endif
</div>
